import 'package:flutter/material.dart';

class SquareTitle extends StatelessWidget {
  const SquareTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
